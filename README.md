# History's Ledger

**Hold both.** Primary-source history — achievement and cost, side by side.  
You read the documents. You decide.

This repo unifies the product and the public site:

| Path | What |
|------|------|
| `content/` | Chapters + primary sources |
| `design/` | Brand, mockups, sample PDF |
| `docs/` | Specs and notes |
| `tools/` | Generators / folio tooling |
| `site/` | Public website (`historysledger.com`) |
| `ATTICUS_SPEC.md` | Atticus guide persona / grounding rules |
| `STATUS.md` | Written vs promised |

Formerly split across `~/american-history-app` and `~/historysledger-site`.  
Powered by SOVERYN (quiet byline — not the product name).

## Local site

See `site/` for the static/Cloudflare Pages front. Do not commit `.env` or `site/.wrangler` secrets.
